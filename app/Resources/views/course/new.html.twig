{% extends 'base.html.twig' %}

 {% block headline %}
     Course creation
 {% endblock %}

{% block content %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('course_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
